// Write a JavaScript program to find the largest of three given integers
function largestInteger(x, y, z) {
    return Math.max(x, y, z);
}

console.log(largestInteger(7, 8, 9));
console.log(largestInteger(17, 12, 44));