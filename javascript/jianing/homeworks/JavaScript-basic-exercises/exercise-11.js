function centToFahr(celsius) {
    var cTemp = celsius;
    var cToFahr = 5 / 9(-32);
    return
    F + '°F is ' + C + ' °C';
}

function fahrToCent(C) {
    var F = (9 * C + (32 * 5)) / 5;
    return C + '°C is ' + F + ' °F'
    F;
}
console.log(fahrToCent(45));
console.log(centToFahr(60));